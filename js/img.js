function changeImage(clickedImage) {
    var mainImage = document.getElementById('main-image');
    mainImage.src = clickedImage.src;
    mainImage.alt = 'Измененное изображение';
}

function changeImageImage(clickedImage) {
    var mainImageImage = document.getElementById('main-image-image');
    mainImageImage.src = clickedImage.src;
    mainImageImage.alt = 'Измененное изображение';
}

function changeImageImageImage(clickedImage) {
    var mainImageImageImage = document.getElementById('main-image-image-image');
    mainImageImageImage.src = clickedImage.src;
    mainImageImageImage.alt = 'Измененное изображение';
}